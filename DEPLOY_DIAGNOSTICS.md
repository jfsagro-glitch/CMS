# 🔍 Диагностика проблем с деплоем

## Возможные причины, почему деплой не проходит:

### 1. ✅ Проверка GitHub Actions
Откройте: https://github.com/jfsagro-glitch/CMS/actions

**Что проверить:**
- Есть ли запущенные workflow?
- Какой статус у последнего workflow (🟢 успешно / 🔴 ошибка / 🟡 в процессе)?
- Если есть ошибка, откройте её и посмотрите логи

### 2. ✅ Проверка настроек GitHub Pages
Откройте: https://github.com/jfsagro-glitch/CMS/settings/pages

**Что проверить:**
- **Source:** должен быть выбран `GitHub Actions` (не `Deploy from a branch`)
- Если Source не настроен, выберите `GitHub Actions` и сохраните

### 3. ✅ Проверка прав GitHub Actions
Откройте: https://github.com/jfsagro-glitch/CMS/settings/actions

**Что проверить:**
- **Workflow permissions:** должно быть `Read and write permissions`
- **Allow GitHub Actions to create and approve pull requests:** должно быть включено

### 4. ✅ Проверка коммитов
Убедитесь, что последние изменения запушены:
```bash
git log --oneline -5
git status
```

### 5. ✅ Возможные ошибки сборки

#### Ошибка TypeScript:
- Проверьте: `npm run type-check`
- Исправьте все ошибки типов

#### Ошибка линтера:
- Проверьте: `npm run lint`
- Исправьте все предупреждения

#### Ошибка сборки:
- Проверьте: `npm run build`
- Убедитесь, что `dist/` папка создана

### 6. ✅ Проверка workflow файла
Файл `.github/workflows/deploy.yml` должен:
- Триггериться при push в `main` или `master`
- Использовать актуальные версии actions (`@v4`, `@v3`)
- Иметь правильные permissions

## 🚀 Решение: Запустить деплой вручную

Если автоматический деплой не запускается, можно:

1. **Создать пустой коммит для триггера:**
```bash
git commit --allow-empty -m "trigger: deploy to GitHub Pages"
git push origin main
```

2. **Или запустить workflow вручную:**
- Откройте: https://github.com/jfsagro-glitch/CMS/actions
- Найдите workflow "Deploy CarShop CMS to GitHub Pages"
- Нажмите "Run workflow" → выберите ветку `main` → "Run workflow"

## 📋 Чеклист для проверки

- [ ] GitHub Pages Source настроен на `GitHub Actions`
- [ ] Workflow permissions настроены на `Read and write`
- [ ] Последние изменения запушены в `main`
- [ ] Локальная сборка проходит успешно (`npm run build`)
- [ ] TypeScript проверка проходит (`npm run type-check`)
- [ ] Линтер проходит (`npm run lint`)
- [ ] В GitHub Actions есть запущенные workflow

## 🔗 Полезные ссылки

- **GitHub Actions:** https://github.com/jfsagro-glitch/CMS/actions
- **GitHub Pages Settings:** https://github.com/jfsagro-glitch/CMS/settings/pages
- **Actions Settings:** https://github.com/jfsagro-glitch/CMS/settings/actions
- **Сайт:** https://jfsagro-glitch.github.io/cms/

