# 🔧 Диагностика проблем с деплоем

## ⚠️ Если деплои не проходят, проверьте:

### 1. ✅ Проверьте GitHub Actions логи

Откройте: **https://github.com/jfsagro-glitch/CMS/actions**

**Что искать:**
- 🔴 Красный X = ошибка (откройте и посмотрите логи)
- 🟡 Желтая точка = в процессе
- 🟢 Зеленый check = успешно

**Частые ошибки:**
- `Build failed` - ошибка сборки (проверьте логи)
- `Deploy failed` - ошибка деплоя (проверьте настройки)
- `Permission denied` - проблемы с правами

### 2. ✅ Проверьте настройки GitHub Pages

Откройте: **https://github.com/jfsagro-glitch/CMS/settings/pages**

**Важно:**
- **Source:** должен быть **"GitHub Actions"** (НЕ "Deploy from a branch")
- Если Source не настроен, выберите "GitHub Actions" и сохраните

### 3. ✅ Проверьте права GitHub Actions

Откройте: **https://github.com/jfsagro-glitch/CMS/settings/actions**

**Workflow permissions:**
- Должно быть: **"Read and write permissions"**
- **Allow GitHub Actions to create and approve pull requests:** включено

### 4. ✅ Проверьте локальную сборку

Выполните локально:
```bash
npm run build
npm run type-check
npm run lint
```

Если есть ошибки - исправьте их перед деплоем.

### 5. ✅ Проверьте workflow файл

Файл `.github/workflows/deploy.yml` должен:
- Триггериться при push в `main` или `master`
- Использовать актуальные версии actions (`@v4`, `@v3`)
- Иметь правильные permissions

### 6. ✅ Проверьте ветку

Убедитесь, что вы пушите в правильную ветку:
```bash
git branch
# Должно быть: * main или * master
```

### 7. ✅ Принудительный запуск деплоя

Если автоматический деплой не запускается:

**Вариант 1: Пустой коммит**
```bash
git commit --allow-empty -m "trigger: deploy to GitHub Pages"
git push origin main
```

**Вариант 2: Ручной запуск через GitHub**
1. Откройте: https://github.com/jfsagro-glitch/CMS/actions
2. Найдите "Deploy CarShop CMS to GitHub Pages"
3. Нажмите "Run workflow"
4. Выберите ветку `main`
5. Нажмите "Run workflow"

## 🔍 Частые проблемы и решения

### Проблема: "Workflow не запускается"
**Решение:** 
1. Проверьте настройки GitHub Pages (должен быть выбран "GitHub Actions")
2. Проверьте, что вы пушите в ветку `main` или `master`

### Проблема: "Build failed"
**Решение:**
1. Откройте логи в GitHub Actions
2. Найдите ошибку (обычно в шаге "Build production")
3. Исправьте ошибку локально
4. Запушьте исправления

### Проблема: "Deploy failed"
**Решение:**
1. Проверьте права GitHub Actions (должны быть "Read and write permissions")
2. Проверьте, что job "build" завершился успешно
3. Проверьте, что artifact был загружен

### Проблема: "Permission denied"
**Решение:**
1. Проверьте настройки Actions: https://github.com/jfsagro-glitch/CMS/settings/actions
2. Убедитесь, что "Workflow permissions" = "Read and write permissions"

## 📋 Чеклист для проверки

- [ ] GitHub Pages Source настроен на `GitHub Actions`
- [ ] Workflow permissions настроены на `Read and write`
- [ ] Локальная сборка проходит успешно (`npm run build`)
- [ ] TypeScript проверка проходит (`npm run type-check`)
- [ ] Линтер проходит (`npm run lint`)
- [ ] Последние изменения запушены в `main`
- [ ] В GitHub Actions есть запущенные workflow

## 🔗 Полезные ссылки

- **GitHub Actions:** https://github.com/jfsagro-glitch/CMS/actions
- **GitHub Pages Settings:** https://github.com/jfsagro-glitch/CMS/settings/pages
- **Actions Settings:** https://github.com/jfsagro-glitch/CMS/settings/actions
- **Сайт:** https://jfsagro-glitch.github.io/cms/

## 💡 Если ничего не помогает

1. Проверьте последние логи в GitHub Actions
2. Скопируйте текст ошибки
3. Проверьте, что все файлы закоммичены и запушены
4. Попробуйте создать новый workflow run вручную

