# 🚀 Как запустить деплой вручную

## ⚠️ Если автоматический деплой не запускается

### Способ 1: Запуск через GitHub Actions UI (РЕКОМЕНДУЕТСЯ)

1. **Откройте GitHub Actions:**
   ```
   https://github.com/jfsagro-glitch/CMS/actions
   ```

2. **Найдите workflow:**
   - В левом меню найдите "Deploy CarShop CMS to GitHub Pages"
   - Или прокрутите вниз до списка workflows

3. **Нажмите "Run workflow":**
   - Справа вверху найдите кнопку **"Run workflow"** (зеленая кнопка)
   - Выберите ветку: **`main`**
   - Нажмите зеленую кнопку **"Run workflow"**

4. **Дождитесь завершения:**
   - Обычно 2-5 минут
   - Проверьте статус в списке запусков

### Способ 2: Проверка настроек GitHub Pages

Убедитесь, что настройки правильные:

1. **Откройте настройки:**
   ```
   https://github.com/jfsagro-glitch/CMS/settings/pages
   ```

2. **Проверьте Source:**
   - Должно быть: **"GitHub Actions"** ✅
   - НЕ должно быть: "Deploy from a branch" ❌

3. **Если Source неправильный:**
   - Измените на "GitHub Actions"
   - Сохраните изменения

### Способ 3: Проверка workflow файла

Убедитесь, что workflow файл существует:

1. **Проверьте файл:**
   ```
   .github/workflows/deploy.yml
   ```

2. **Файл должен содержать:**
   - Триггеры: `push`, `pull_request`, `workflow_dispatch`
   - Job: `build` и `deploy`
   - Правильные permissions

## 🔍 Проверка статуса

**GitHub Actions:**
https://github.com/jfsagro-glitch/CMS/actions

**Ожидаемый workflow:**
- Название: "Deploy CarShop CMS to GitHub Pages"
- Статус: 🟡 В процессе → 🟢 Успешно

## 📝 После успешного деплоя

Сайт будет доступен:
https://jfsagro-glitch.github.io/cms/

## ⚠️ Если ничего не помогает

1. Проверьте, что все коммиты запушены:
   ```bash
   git log --oneline -5
   git push origin main
   ```

2. Проверьте, что workflow файл на месте:
   ```bash
   ls -la .github/workflows/deploy.yml
   ```

3. Создайте новый коммит:
   ```bash
   git commit --allow-empty -m "trigger: manual deploy"
   git push origin main
   ```

