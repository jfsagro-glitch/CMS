## DeepSeek Cloudflare Worker Proxy

В продакшене фронтенд не может напрямую вызывать DeepSeek API (браузер блокирует CORS).  
Решение — лёгкий Cloudflare Worker, который принимает запросы от фронта, вставляет секретный API‑ключ и проксирует вызов на `https://api.deepseek.com/v1/chat/completions`.

Ниже — полный чеклист, как поднять такой воркер “с нуля”.

---

### 🔧 Что понадобится

1. Аккаунт Cloudflare с включенной функцией Workers (бесплатного плана достаточно).
2. Node.js + npm на вашем компьютере.
3. Утилита [`wrangler`](https://developers.cloudflare.com/workers/wrangler/install-and-update/):
   ```bash
   npm install -g wrangler
   ```
4. Клон этого репозитория (нам нужна папка `proxy/`).

---

### 🏗 Шаг 1. Подготовить конфигурацию

1. Перейдите в папку `proxy/`.
2. Скопируйте файл конфигурации:
   ```bash
   cp wrangler.example.toml wrangler.toml
   ```
3. Откройте `wrangler.toml` и:
   - укажите уникальное имя воркера (поле `name`);
   - при необходимости поправьте `compatibility_date`.

> ⚠️ Секреты в `wrangler.toml` не прописываем, их задаём отдельно (см. шаг 3).

---

### 🧪 Шаг 2. Протестировать локально (по желанию)

```bash
wrangler dev --local
```

В ответ появится локальный URL (например, `http://127.0.0.1:8787`).  
Можно отправить тестовый запрос `curl` с телом `chat/completions`, чтобы убедиться, что воркер отвечает.

---

### 🔐 Шаг 3. Задать секреты в Cloudflare

1. Авторизуйтесь в Cloudflare:
   ```bash
   wrangler login
   ```
2. Добавьте секрет `DEEPSEEK_API_KEY` (сам ключ вводится только в консоли и не попадёт в репозиторий):
   ```bash
   wrangler secret put DEEPSEEK_API_KEY
   ```
3. (Опционально) добавьте `ALLOWED_ORIGINS`, если хотите ограничить список доменов:
   ```bash
   wrangler secret put ALLOWED_ORIGINS
   # Пример значения: https://cmsauto.ru,https://jfsagro-glitch.github.io
   ```

---

### 🚀 Шаг 4. Деплой воркера

```bash
wrangler deploy
```

После успешного деплоя в консоли появится адрес вида  
`https://your-worker-name.your-account.workers.dev`.

Скопируйте этот URL — он понадобится фронтенду.

---

### 🔗 Шаг 5. Связать фронтенд с прокси

1. В корне проекта создайте файл `.env.production` (или используйте существующий):
   ```
   VITE_DEEPSEEK_PROXY_URL=https://your-worker-name.your-account.workers.dev
   ```
2. Выполните сборку и деплой CMS (`npm run build && npm run deploy`).

Фронтенд автоматически начнёт отправлять запросы к DeepSeek через воркер (см. `src/services/DeepSeekService.ts`).

---

### ✅ Готово

Теперь:

- Production использует Cloudflare Worker и не упирается в CORS.
- Development (localhost) продолжает работать через Vite-прокси автоматически.
- Секретный ключ хранится только в Cloudflare, в браузер не попадает.

Если понадобится другой хостинг (Vercel / Netlify functions / свой backend), можно взять этот же код воркера и адаптировать к нужной платформе — логика проксирования остаётся прежней.
